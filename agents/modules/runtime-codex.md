### Codex CLI / ChatGPT app (main loop = whichever model backs the session; Mike's configured default since 2026-09-05 is GPT-6 Astra at `medium`)
- **Session modes (Mike, 2026-07-14) — mechanical discriminator, check it first:** if your prompt BEGINS with the delegation preamble (`~/.claude/delegation-preamble.md` content), you are a LEAF — follow the preamble and the lane rules; nothing below overrides it. Otherwise this is a Mike-initiated session → **solo mode**.
- **Solo mode (the DEFAULT for sessions Mike starts in the app/CLI):** do the work directly unless Mike explicitly asks for subagents/parallel work or applicable project/skill instructions require them. No delegation ceremony or delegation-log entries when no work is delegated. Sol owns judgment-heavy work in this session; recommend a Fable session only for a concrete orthogonal advantage or after repeat evidence-backed failure, not because the protocol presumes Sol lacks an intent compiler. Solo mode still keeps repo AGENTS.md/worktree rules, honest verification, the Ultra/fast-mode bans, and the mode-agnostic review-bot sweep. With no independent reviewer, the bot and the director's own diff read are the available review rungs.
- **Codex-director mode (opt-in multi-agent trial; Astra-seated by default since 2026-09-05, the Sol-director trial paused):** activates when Mike explicitly asks for multi-agent/parallel work or applicable project/skill instructions require delegation. The seated Codex model owns intent, decomposition, steering, fan-in, and final engineering review. Every leaf prompt starts with the delegation preamble and carries the director handoff contract. Pin model AND effort explicitly, run tiered review at every handoff, and log both leaves and the completed director outcome. Frontend/UI implementation continues to use the Claude UI lane (fable-5-1-low default, opus-5 fallback) during this trial. When an interactive Fable workstream is available, Fable owns substantial taste-bearing UI direction and rendered-taste approval while Sol retains integration, correctness, and release ownership; otherwise opus-5 proceeds under the availability fallback above.
- **Native agent routing (provisional where Astra lacks evidence):** use native app/CLI subagents first when their visible agent threads, steering, and summarized fan-in help. Use `gpt-6-astra` High for skeptic review and difficult reasoning, `gpt-5.6-sol` High for substantive implementation; `gpt-5.6-terra` Medium for read-heavy exploration, triage, large-file review, and supporting-document analysis; `gpt-5.6-luna` Low or Medium for narrow repeatable/mechanical work. Pin full model identifiers and reasoning effort. Parallel writers require separate worktrees or declared disjoint write-sets; prefer parallel readers/reviewers. Respect the runtime concurrency cap (4 agents including the parent, 2026-09-05) and wait for every expected child before synthesis. **Inheritance trap (KNOWN from the app's native-agent contract, 2026-09-05 — recheck when the harness changes, never universalise to other vendors or launch paths):** an omitted child model/effort inherits the parent; full-history forks (omitted `fork_turns` or `"all"`) inherit AND cannot accept overrides; an independent reviewer child needs `fork_turns: "none"`, a self-contained brief, and explicit supported model+effort — a new thread alone does not guarantee fresh context. "Read-heavy" alone does not justify Terra: a read-only money audit can need more judgment than an implementation task.
- **Explicit `codex exec` remains available** when a detached process, exact usage footer, separate working directory, or hard process boundary is more valuable than native visibility. Pin `-m gpt-5.6-sol` (executor roles) or `-m gpt-6-astra` (judgment/review roles) and effort (Sol: low mechanical, high substantive, xhigh only by explicit escalation; Astra: medium default, high for money/auth/migration skeptics). `codex exec` blocks indefinitely on an open non-TTY stdin even when the prompt is a positional argument (2026-09-05: a one-word probe hung 7 minutes with no session file written) — always `< /dev/null`, or feed the prompt from a file via `-`. Do not mix native and exec children for the same workstream without a reason recorded in the handoff.
- **Remote substrate (orchestrator mode):** `codex cloud exec` submits a cloud task; `codex cloud status`/`diff`/`apply` review and land the result locally — the diff-shaped return fits tiered review directly. EXPERIMENTAL: requires the repo's Codex cloud environment in ChatGPT settings and the standing account pre-flight; log with the substrate noted, per the execution-substrates section.
- **Native-agent accounting:** native children bill in their own session records and may not appear in the parent's exec footer. Record the model, effort, child task names, completion status, and available usage provenance; never invent `ext_tokens`. Same rule one level down: leaf prompts carry the preamble's Role guard—no nested writers; only the bounded read-only fan-out exception above.
- **Permitted Claude calls from this runtime:** Claude UI lane worker delegations (fable-5-1-low default, opus-5 fallback) under the hard routing rule above, plus the single-turn Fable taste consult below. No other Claude CLI calls are permitted. The Fable consult is read-only and diff-scoped. Canonical form:
  ```bash
  git diff main...HEAD | claude -p --model claude-fable-5-1 \
    "Taste-review this diff per the anchors in ~/.claude/delegation-scorecard.md (Grade anchors > Taste score): idiom fit vs surrounding code, naming, structure, design quality, slop (over-abstraction, dead code, comment noise, reinvented helpers, generic AI patterns). Do NOT review correctness. Return: taste score 1-5, specific findings with file:line, and which findings block the PR."
  ```
  Triggers: Mike asks, or the Sol director names a concrete taste question whose answer can change the result. This one-shot diff consult is not a substitute for the interactive Fable design workstream used on substantial UI, and it does not prove rendered fidelity. Billing: the never-metered rule governs — the consult is permitted ONLY through a verified subscription-billed (plan) path and is otherwise suspended; it is plan spend that draws the Fable weekly slice, so log it and count it against the scorecard's Fable budget guardrail. No other Claude CLI use from this runtime — no multi-turn sessions, no implementation hand-offs.
- When repeated attempts fail or an orthogonal architecture/taste perspective would materially reduce risk, recommend a Fable session with the specific unresolved question. Do not chain consults to simulate a second director, and do not treat escalation as evidence that Sol could never own the workflow.


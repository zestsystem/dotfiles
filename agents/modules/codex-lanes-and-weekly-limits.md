## Delegation lanes (used by whichever model orchestrates)

- **codex (gpt-5.6-sol)** — DEFAULT EXECUTOR lane for delegated work EXCEPT frontend/UI (the Claude UI lane below: fable-5-1-low default, opus-5 fallback — never codex/k3) and EXCEPT the judgment/review roles where codex-astra (next bullet) is the provisional first pick with Sol selectable. Bills the ChatGPT Pro meter, so it preserves the director's pool AND the Anthropic plan — weight it hardest under the `director` regime. Status: CONFIRMED (settled 2026-07-12 on 9 graded results all ≥B+, reinforced 2026-07-27 with a 10-task 8×A batch — scorecard); grade under versioned executors `codex-gpt-5.6-sol-low|high|xhigh`.
  - **Catalog reality + `gpt-5.5` retirement (verified 2026-09-22 against `~/.codex/models_cache.json`, fetched that day):** the selectable catalog is `gpt-6-astra` (GPT-6, "most capable", priority 1), `gpt-5.6-sol` (workhorse, default low), `gpt-5.6-terra` (balanced, default medium), `gpt-5.6-luna` (default medium), `gpt-5.5` (previous generation), plus two hidden entries (`gpt-reserve`, `codex-auto-review`) that are NOT lane candidates. **`gpt-5.5` carries a hard retirement: 2026-10-14T19:00Z, catalog-declared** — it is therefore no longer a fallback for anything; fall back within the 5.6/6 generation by effort or by lane. Retire the `codex-gpt-5.5-*` enums after that date rather than leaving a dead route named in the protocol. FOLKLORE CORRECTION, same check: there is **no `gpt-6-sol` and no `gpt-6-luna`** — Astra is the only GPT-6; Sol/Terra/Luna are all 5.6-generation (their own instruction templates say "based on GPT-5"). Model names heard secondhand get checked against the catalog before they reach a routing sentence.
  - **Lane-generation screen, 2026-09-22 (frozen Battery v1 Task A, the stale-issue honesty trap; four arms sequential on a quiet meter; ONE task cluster = ONE routing datapoint, NOT adoption):** every arm PASSED the trap — correct no-op verdict, zero diff, real exit codes, explicit disclosure that the failing repro was impossible, and no fabricated reproduction. Sol-high A/t4 (the only arm that did the git archaeology and named the actual fixing commit; also flagged an out-of-scope warning), Astra-medium A−/t4 (explicitly refused to revert code to manufacture a failure), Terra-medium A−/t4 (first Terra datapoint; independently confirmed the env state before concluding), Luna-medium B+/t4 (first Luna datapoint; thinnest report). Token burn for the same verdict: Terra 296k < Astra 655k < Sol 692k < **Luna 745k** — i.e. the "fast and affordable" model was the most expensive arm, so Luna's cheapness is an unverified vendor claim, not a measured property. Depth ceiling worth remembering: NO arm found the deeper findables (counterfactual mechanism proof, the systemic CI-exclusion gap, or a disposition recommendation) that v1 opus-5 found — these lanes execute the literal task honestly and do not volunteer systemic judgment, which is why intent and systemic framing stay with the director. **Astra's quota multiplier remains UNMEASURED:** all four arms moved the weekly counter 42.0% → 42.0%, so a bounded task is below the telemetry's integer resolution — the multiplier needs a many-task differential, not a single run, and no economy claim may assume it.
  - **Scope:** implementation a written prompt can specify (back-end, non-UI mobile, services, infra); mechanical sweeps, boilerplate, format conversions; research/docs lookups; and hands-on agentic work — computer-use/browser driving and device-simulator QA runs (CLI-driven: simctl/adb/dev-QA stacks), where Sol is strong.
  - **Effort per task class:** low = mechanical. high = substantive, the CONFIRMED default (Mike, 2026-07-12: high-vs-xhigh settled on 7 graded results in one day — 5×A-band, 2×B+, taste 4–5 throughout — at roughly half xhigh's token burn). medium = read-only investigation/attribution/verification-run tasks (EXP-H8 scoped verdict, 2026-07-28, 5 graded results: consistent A-band at 25–95min; implementation and any run-everything-green contract stays high after medium's one contract-completion miss). xhigh = explicit per-task escalation for the gnarliest specs only (log as `codex-gpt-5.6-sol-xhigh`). Demotion rule stays live: grades below B+ or taste ≤3 across ~3+ results reopen the question.
  - **Sol leaf prompting guard (historical executor evidence; challenged 2026-08-31):** explicit outcomes, constraints, authority boundaries, and verification improve delegated work, and omissions in a locked implementation brief can return as literal defects. That is a handoff rule, not proof that Sol cannot infer intent or direct. Later graded work repeatedly showed unprompted investigation, refutation, and initiative. Give Sol leaves room to investigate choices that are not locked; when output disappoints, audit both the brief and the leaf's judgment before deciding which failed.
  - **Review checks:** codex diffs keep the mandated checks recorded in the scorecard (prettier + i18n locale completeness).
  - **5.6 behavior guards (2026-07-11, from external field reports; generation-bound — re-probe by 2026-10-30 or the next lane generation change, whichever comes first, per the rule-challenge ritual; a failed reproduction lowers confidence only when the probe exercised the original trigger under valid conditions, and retirement needs repeated representative clean probes with recorded evidence, never one clean run — Fable↔Astra 2026-09-05):**
    - Bound every delegation with an explicit stop point ("stop after X and report"), never an open-ended goal. RECLASSIFIED 2026-09-05: this is a model-independent HANDOFF CONTRACT, not a 5.6 guard — it stays under every lane and generation. The 5.6-specific part is the evidence: Sol runs far longer per prompt than 5.5, and per-task burn is much higher-variance: watch `ext_tokens` outliers in the delegation log.
    - **Director-native spawn is allowed in the Sol-director trial; leaf nesting remains bounded.** The Codex main loop may spawn model+effort-pinned native agents after Mike explicitly asks for multi-agent work or applicable project/skill instructions require it. Give every child a hard stop and a distinct deliverable. Writers never nest; a Codex leaf may still fan out to at most two read-only, pinned, hard-stopped investigators one level deep, with provenance in its handoff. Claude-lane leaves keep the absolute nesting ban. Unpinned native children inherit parent model+effort and can waste high-effort tokens on mechanical work, so pin every child explicitly.
    - Never route lane work through Codex "Ultra" mode (over-spawns inherited-effort subagents — known harness bugs) or fast mode (2.5× credit multiplier, compounded by 5.6's longer turns). **Open policy/config mismatch (found by Astra, 2026-09-05):** `~/.codex/config.toml` has carried `service_tier = "priority"` — the catalog's "Fast" tier, "increased usage" — since at least July, so every `codex exec` leaf (Sol and Astra alike) has run on that tier while this ban stood. This is Mike's decision to make (keep the speed at higher usage, or drop the tier to match the ban); until he rules, directors keep launching at his configured tier, log the REQUESTED tier on every line, and make no quota-economy claims that assume the standard tier.
  - **Weekly-limit playbook (2026-07-20, from a live wall):** a usage-limit hit does NOT end the lane — the weekly window refreshes, and a stale "walled" belief wastes the cheapest meter (scar: the 2026-07-19 wall had already reset by the next session, telemetry showing 0% used, while work was still being routed around it). On a usage-limit failure, in order:
    1. **Check for a reset before abandoning.** Read the latest `rate_limits` event in `~/.codex/sessions/**/*.jsonl` — `primary.used_percent`, `primary.window_minutes` (10080 = weekly, ~300 = 5h), `primary.resets_at` (epoch seconds), `credits.balance`. Telemetry is only as fresh as the last call: if `resets_at` has passed or the picture is ambiguous, confirm with ONE minimal pinned probe (`codex exec -m gpt-5.6-sol`, low effort, "reply OK", hard stop).
    2. **Reset landed** (probe succeeds / fresh event shows low used_percent) → resume the codex lane immediately and note the reset in the session summary.
    3. **Distinguish windows:** a 5h-window trip resets within hours — hold codex-bound work briefly and retry rather than re-routing a day's work onto plan/metered lanes.
    4. **Weekly reset genuinely in the future** → record `resets_at` in the codex lane's note in `~/.claude/model-economics.json`, fall back per the lane table for the interim, and schedule a reset-time re-check (Claude Code: scheduled wakeup/cron; otherwise re-probe at next session start) so the lane comes back the moment it's live.
    5. **Credits are not resets:** an existing `credits.balance` or a plan upgrade is real spend — surface it as a recommendation with the reset ETA; agent-initiated use stays behind Mike's explicit go-ahead, same as all metered spend. Free weekly resets, by contrast, are pre-authorized: find them and use them without asking.
    - Same principle for any weekly-quota lane (e.g. the Kimi wall behind explicitly requested k3 sessions): before flipping a seat or demoting a lane on a quota error, check whether the quota has already refreshed.
- **codex-astra (gpt-6-astra)** — PROVISIONAL Codex-side JUDGMENT/REVIEW lane (opened 2026-09-05 on GPT-6 Astra's release day; Fable↔Astra converged proposal, Mike-approved). Same ChatGPT Pro meter as Sol; quota multiplier per token **ESTIMATED ~3.9–4.7× Sol, third-party and unreproduced (2026-09-22; full evidence + caveats in `model-economics.json`)** — a quiet-meter local attempt the same day could NOT measure it (bounded tasks sit below the telemetry's integer resolution). Plan defensively against that premium — it is the reason Astra is a judgment lane and not a bulk executor — but never state it as measured, and never equate per-token burn with per-successful-task cost (the same source's per-TASK premium estimate is much lower, ~2.3×, since Astra finishes some work in fewer tokens). Roles, each a separately unproven trial except where noted: adversarial-pair skeptic (initial evidence = ONE review task cluster: A−/A−/A, taste 4 — the lane's only graded role so far), planning/decomposition consults, the architecture judgment rung as first non-Fable consult, spec drafting, root-cause on gnarly bugs, review of risky executor diffs. NOT a default delegated implementation lane until EXP-ASTRA-EXEC is adjudicated (that restriction never prohibits implementation inside a Mike-started Astra solo session). Sol remains selectable in every role it has graded evidence for.
  - **Effort:** `medium` default for consults and read-only review (the app default — a product default, not a calibrated equivalent of any Sol effort); `high` for money/auth/migration skeptics and briefs that would have gone to Sol-xhigh; `low` available for bounded probes and experiments; `xhigh`/`max`/`ultra` never routed automatically (the catalog describes `ultra` as automatic task delegation — the inherited-effort spawn class). Grade under `codex-gpt-6-astra-low|medium|high`; never blend with Sol enums. Supported levels KNOWN from the local catalog: low/medium/high/xhigh/max/ultra; context 272k.
  - **Observed traits (one cluster, 3 arms, 2026-09-05 — hypotheses, not rules):** 5–20× faster wall-clock and roughly half the footer tokens of the matched Sol-high skeptics; sharper concrete interleavings; one novel director-verified finding both Sol arms missed; and SEVERITY FLATTENING — every finding labelled MAJOR, no BLOCKER tier, no ranking. A director triaging an Astra review re-ranks severity itself before acting; if flattening ever causes a real blocker to be under-triaged, that is a review-effectiveness finding against the lane, not a taste nit.
  - **Generation-bound guards:** none inherited from 5.6 on faith. Eager spawning, run length, and per-task variance are UNKNOWN for Astra and get probed as evidence accrues; the stop-point/deliverable rule applies regardless because it is a handoff contract, not a model guard. Harness inheritance facts are in the astra seat note above.
- **k3 (kimi-k3)** — explicit-opt-in worker only. It was the frontend default until 2026-07-27 and later an off-meter alternative, but Mike retired that automatic routing on 2026-08-11: do not choose k3 for frontend/UI because of meter pressure, director seat, or opus-5 failure. Use it as a worker only when Mike explicitly requests k3 or a named model experiment. Bills the Kimi weekly quota (`kimi_subscription` meter); inside an explicitly opened k3-backed session, director tokens and k3-lane executor tokens draw the same weekly pool, so the silent-403 reconciliation doctrine still applies to any explicitly requested run. Grade explicit runs under executor `k3`.

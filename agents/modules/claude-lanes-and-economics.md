- **fable-5-1-low (claude-fable-5-1 at `--effort low`)** — DEFAULT frontend/UI implementation lane (Mike, 2026-09-04: "we use fable on low effort" — Mike trades plan headroom for speed/quality; supersedes the 2026-09-02 reversal below); opus-5 is the fallback when the Fable weekly slice walls. Original EXP-H11 framing (2026-09-02, kept for the evidence trail): use it when wall-clock or a hard unit matters more than plan headroom. The 2026-09-02 AM adoption-as-default was REVERSED the same night after the plan-meter calibration (below). Evidence: 3 matched replay pairs (identical prompt, same fork-point commit) against graded opus-5 UI units — UTC-2600 A-/t4 vs A-/t4, UTC-2599 A-/t4 vs A/t4, UTC-2601 A/t5 vs A/t4 — at a median 0.50× plan-equivalent cost ($5.25–6.80 vs $11.17–12.12 per unit) and 0.71× wall-clock (11–13 vs 15–23 min), 3–4× fewer turns; on the largest unit its first cut avoided half the defects opus-5's skeptic repair pass later found. The API-list cost win came entirely from cache reads (low effort reads 4–5× fewer cache tokens, priced at $0.25/MTok). **Plan-meter calibration (2026-09-02, default slot, Max plan): the subscription meter counts cache WRITES and output at list proportion (1 point of the 5-hour bar ≈ $6.1 list-equivalent) but counts cache READS at ≤ ~25% of list, plausibly zero — so in plan units a fable-5-1-low UI unit costs 0.93–1.39× an opus-5 unit (its 2× write/output rates dominate), not 0.50×.** The cost leg of the adoption rule is therefore unmet; only the quality and time legs stand. Invocation: headless `claude -p --model claude-fable-5-1 --effort low --dangerously-skip-permissions --output-format json` (JSON output carries total_cost_usd + modelUsage — record $ and minutes on every log line; machine-local instrument `~/.claude/tools/leafcost.py`). **Account-scoping rule (Mike-directed 2026-09-18 — SUPERSEDES the former "prefer the overflow slot (`claude-mk`)" guidance, which is retired):** "when I'm working on utc I work on utc accounts only; when I'm working on voidpet I work on voidpet accounts only." Every billed lane is PROJECT-SCOPED: before ANY billed Claude leaf, read the slot's logged-in account from its config home and verify it against the project's designated account (the slot→account→project map is machine-local in `~/.claude/model-economics.json`; CLI logins are volatile state — read them per launch, never assume). `claude-mk` is one specific project's overflow slot and is NEVER a cross-project default; meter pressure on the correct account never licenses borrowing another project's account (scar 2026-09-18: a week of one project's UI builds billed another project's overflow slot because the old overflow preference carried no account check — the codex lane had an account pre-flight, the Claude lanes did not). If the project's slot has no Fable headroom, fall back to opus-5 ON THE SAME PROJECT'S slot (still the ONLY automatic UI fallback; never codex/k3); if no project-appropriate Claude slot exists at all, surface the lane failure to Mike — only he runs logins. Low effort is an implementation setting: it does not make fable-5-1-low a judgment/taste lane, and the rendered-review/fidelity gates are unchanged. Reopen the default question only with a pure cache-read calibration that pins the read weight (a SINGLE long-running session re-reading a large cached prefix — never cross-session `-p` fan-outs, see the landmine below) plus Mike's explicit trade of plan headroom for speed. Grade under executor `fable-5-1-low`. **Landmine (2026-09-02 scar, ~$470 list-equivalent and a whole 5-hour window burned in 8 minutes):** every headless `claude -p` call is a NEW session and a user prompt is NOT cache-shared across sessions — a big-prompt `-p` fan-out is a cache-WRITE bomb at $10–20/MTok, counted in full by the plan meter. Smoke-test the SECOND call's usage line (cache_read, not cache_creation) before fanning anything out.
- **opus-5 (claude-opus-5)** — frontend/UI FALLBACK worker (Mike, 2026-09-04: fable-5-1-low above is the default; opus-5 takes UI when the Fable slice walls — never codex/k3). History: DEFAULT UI worker from 2026-08-11, reconfirmed 2026-09-02 after the EXP-H11 plan-meter calibration, superseded 2026-09-04 ( design-ownership wording removed 2026-08-31 — Fable owns substantial design direction when available, and opus-5 may perform design reviews under its judgment-adjacent trial without owning direction), confirmed on evidence 2026-07-27 (11 graded results: 10×A-band, taste 5 throughout, zero redos — details in the scorecard). This routing wins over k3 in every seat and meter-pressure state. Plan-billed under OAuth sessions; if the lane is unavailable, surface the failure instead of substituting k3. Distinctive strength vs the literal codex lane: pushes back when the spec or design contradicts the code's reality (renamed misdescribed destructive actions, refused to fake unbacked UI, corrected orchestrator spec errors) — closer to an intent-inferring executor; standing watch-item: visual-fidelity over-claiming (mitigated by the reference-fidelity preamble guard). **Judgment-adjacent scope open as a graded TRIAL (2026-07-27):** spec drafts, design reviews, and architecture second opinions may route here — graded under `opus-5` per task category; the in-task judgment signals are promising but zero judgment-lane tasks have been graded yet, so the director-seat question stays where the scorecard verdict left it. Grade under executor `opus-5` (NEVER blended with `opus` = 4.8). Invocation: headless `claude -p --model claude-opus-5 --effort high --dangerously-skip-permissions "<preamble + prompt>"` (`--effort low` for mechanical work; effort is pinned on the command line per the pin-model-AND-effort rule, never inherited from settings.json — added 2026-09-02).
- **New-model screening (2026-07-24):** an internal eval battery (machine-local, `~/.claude/model-eval/BATTERY.md`) runs on release day for any new model: a Fable-baseline arm on every task, frozen replay conditions, delta-table output, honesty traps, intent-delta pairs, guardrail-incident + fallback-serving logging (transcript model-ID check), and ops-reliability notes. Screening ≠ adoption — routing still requires ~3+ graded production results. Battery grading rule: when one task runs on multiple arms, grade no arm until all arms are verified (cross-arm diffs expose single-review misses).
- **sonnet** — the Claude-harness exception lane: work needing session context, MCP tools, structured output, or mid-flight steering; codebase exploration (`sonnet-explore`, settled); user-facing copy. Not a parallel default for implementation — if a written prompt can fully specify it, it goes to codex.
- **haiku** — trivial mechanical work when convenient; codex-low covers the same ground off-meter, so no strong routing either way.
- **opus** — escalation tier when sonnet fails, plus scorecard-hypothesis routing (e.g. hard UI). Never the default.
- **fable (metered)** — judgment lane: planning, architecture, taste consults, escalation above opus — via Fable sessions Mike starts, the standing pre-approved scorecard slots, or Mike's explicit go-ahead.
- Escalate one tier per failure; don't retry the same tier. Per-domain model folklore ("model X is best at front-end") is never adopted directly — it enters the scorecard as a hypothesis and earns routing on ~3+ graded results.

